# Stage 1 - one pixel

built single TX/RX pair, NoIR cam in the dark box.

- finger gives a clear bump, ~41 counts over baseline.
- baseline noise sigma ~1.4. so SNR ~29. way over the >=5 target.
- touched and untouched ranges don't overlap across 10 cycles. good.
- 850nm through 1mm POF was fine over ~30cm, didn't need the red-debug trick.

exit: PASS. moving to 2x2.
